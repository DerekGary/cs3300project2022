<!DOCTYPE html>
<html lang="en">
  <head>
  <%= render "general/boot-header" %>
    <title>CS3300 Portfolio Project</title>
  </head>
    <body class="bg-dark text-white">
      <div class="modal modal-signin position-static d-block py-5" tabindex="-1" role="dialog" id="modalSignin">
        <div class="modal-dialog" role="document">
        <div class="modal-content rounded-1 bg-secondary">
        <div class="modal-header p-5 pb-4 border-bottom-0 d-block text-center">
          <h4>CS 3300 Portfolio Project - Fall 2022</h4>
        </div>
        <div class="modal-body pt-0 text-center">
          <div style="padding-left: 2rem;">
          <h5 class="pb-5"> Student: Derek Gary </h5>
        </div>
        <div class="modal-footer d-flex justify-content-center ">
        <% if user_signed_in? %>
          <div> Welcome <%= current_user.email %> </div>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %> 
        
        <% else %>
          <a href="users/sign_in" class="btn btn-lg btn-success btn-block"> Sign in</a>
        <% end %>
      </div>
        </div>
        </div>
        </div>
    </div>
  </body>
</html>