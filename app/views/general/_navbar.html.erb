<nav class="navbar fixed-top navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
        <ul class="navbar-nav">
            <li class="nav-item">
                <%= link_to "Home", root_path, class: "nav-link #{'active font-weight-bold'}" %>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#"
                    role="button"
                    data-bs-toggle="dropdown">Account</a>
                <ul class="dropdown-menu">
                <% if user_signed_in? %>
                    <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-default btn-sm text-dark"  %>
                    <%= link_to "New Project", new_project_path, class: "nav-link text-dark #{'active font-weight-bold' if current_page?(new_user_session_path)}" %>
                <% else %>
                    <%= link_to "Log in", new_user_session_path, class: "nav-link text-dark #{'active font-weight-bold text-dark' if current_page?(new_user_session_path)}" %>
                    <%= link_to "Sign up", new_user_registration_path, class: "nav-link text-dark #{'active font-weight-bold text-dark' if current_page?(new_user_registration_path)}" %>
                <% end %>
                </ul>
            </li>
        </ul>
    </div>
</nav>