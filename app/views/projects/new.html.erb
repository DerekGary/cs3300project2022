<head>
  <%= render "general/boot-header" %>
</head>

<%= render "general/navbar"%>

<h1>New project</h1>

<%= render "form", project: @project %>

<br>

<div>
  <%= link_to "Back to projects", projects_path %>
</div>
